// Temporarily disabled commands — kept in the tree but not exposed via the CLI
#[allow(dead_code)]
pub mod add;
pub mod auth;
pub mod build;
pub mod bundle;
pub mod docs;
pub mod new;
pub mod project;
// Temporarily disabled commands — kept in the tree but not exposed via the CLI
#[allow(dead_code)]
pub mod publish;
pub mod run;
pub mod serve;
pub mod wit;
// Temporarily disabled commands — kept in the tree but not exposed via the CLI
#[allow(dead_code)]
pub mod update;
pub mod upgrade;
